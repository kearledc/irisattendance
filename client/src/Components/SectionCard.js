import React from "react";
import { Link } from "react-router-dom";
// Semantic UI

const SectionCard = () => {
	// console.log(section);
	return (
		<div>
			<h1>Hello from sections</h1>
		</div>
	);
};

export default SectionCard;
